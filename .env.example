# UAIMS Environment Configuration
# Copy this file to ".env" and adjust values as needed.

# Organization name shown in the UI and generated reports
ORG_NAME=PAF-IAST

# SQLAlchemy database URL. Defaults to a local SQLite file if not set.
# DATABASE_URL=sqlite:///database/inventory.db
